// state.js — owns the `resume` object (single source of truth) and the only
// functions allowed to mutate it (updateField, addExperience, removeExperience, ...).
// Nothing outside this file should touch `resume` directly.
